# 健康检查功能说明

## 功能概述

健康检查模块会自动定期检测 API 的响应状态，当检测到 API 无响应时，会自动重启对应账号的 bridge 连接，确保服务持续可用。

## 核心特性

### 1. 定时检测
- **间隔时间**：每 5 分钟执行一次健康检查
- **检测方式**：通过本地 API 发送测试请求
- **测试模型**：`mimo-v2.5-pro`
- **测试消息**：`"你是谁"`
- **超时时间**：30 秒

### 2. 快速检测
- Bridge 上线后 **15 秒**立即执行首次检测
- 确保新节点立即可用，快速发现问题

### 3. 自动修复
- **失败阈值**：连续 2 次检测失败
- **修复动作**：
  1. 主动断开旧的 WebSocket 连接
  2. 获取全局创建锁（防止其他账号误创建）
  3. 触发该账号的重建事件
  4. 等待节点重新上线（最多 10 分钟）
  5. 释放创建锁

### 4. 隔离性
- 每个账号独立监控
- 单个账号的重启不影响其他账号
- 使用全局创建锁保证同一时刻只有一个账号在创建/重启

## 配置说明

健康检查使用 `.env` 文件中的配置：

```bash
# API Key（必需，用于测试请求）
MIMO_RELAY_OPENAI_KEY=sk-your-random-secret-here

# 网关地址（健康检查会连接此地址）
SERVER_HOST=127.0.0.1
SERVER_PORT=23655
```

如果未配置 `MIMO_RELAY_OPENAI_KEY`，健康检查会自动跳过。

## 工作流程

```
[Bridge 上线]
    ↓
[等待 15 秒]
    ↓
[执行首次健康检查] ←─────┐
    ↓                     │
[检测成功？]              │
    ├─ 是 → [等待 5 分钟] ─┤
    └─ 否 → [累计失败次数]
                ↓
        [失败次数 ≥ 2？]
            ├─ 否 → [等待 5 分钟] ─┐
            └─ 是 → [重启 Bridge]  │
                        ↓          │
                [等待节点上线]      │
                        ↓          │
                [重置失败计数]      │
                        ↓          │
                [等待 15 秒] ───────┘
```

## 日志示例

### 正常检测
```
[账号 1234567890] 开始 API 健康检查...
[账号 1234567890] ✅ API 健康检查通过 (耗时 1.23s, 响应长度 156 字符)
```

### 检测失败
```
[账号 1234567890] ❌ API 健康检查失败: HTTP 503, 响应: Gateway Error: 没有可用的内网节点
[账号 1234567890] 健康检查失败 (连续 1 次)
```

### 触发重启
```
[账号 1234567890] 连续 2 次健康检查失败，触发 bridge 重启
[账号 1234567890] 🔄 API 无响应，准备重启 bridge...
[账号 1234567890] 🔌 主动断开旧 WebSocket 连接...
[账号 1234567890] 已断开 1 个旧连接
[账号 1234567890] 🔒 等待获取全局创建锁...
[账号 1234567890] ✅ 已获取全局创建锁，触发重建事件
[账号 1234567890] 已设置重建事件，等待节点重新上线...
[账号 1234567890] ✅ Bridge 重启成功，节点已重新上线
```

## 测试方法

使用提供的测试脚本验证健康检查功能：

```bash
# 确保服务正在运行
python main.py

# 在另一个终端运行测试
python test_health_check.py
```

测试脚本会：
1. 检查环境变量配置
2. 向本地 API 发送测试请求
3. 显示检测结果

## 常见问题

### Q1: 健康检查一直显示"未配置，跳过"？
**A**: 检查 `.env` 文件中是否配置了 `MIMO_RELAY_OPENAI_KEY`。

### Q2: 健康检查失败但 API 实际可用？
**A**: 可能是以下原因：
- 所有节点正在冷却中
- 网关正在重启
- API Key 不正确
- 测试模型 `mimo-v2.5-pro` 不可用

### Q3: 重启 bridge 后仍然失败？
**A**: 可能是账号本身的问题：
- 账号被风控
- 容器创建失败
- 网络连接问题
- 检查日志中的具体错误信息

### Q4: 如何调整检测间隔？
**A**: 修改 `mimo2api/health_checker.py` 中的常量：
```python
HEALTH_CHECK_INTERVAL = 300  # 5 分钟（单位：秒）
HEALTH_CHECK_QUICK_DELAY = 15  # 快速检测延迟（单位：秒）
```

### Q5: 如何调整失败阈值？
**A**: 修改 `mimo2api/health_checker.py` 中 `monitor_account` 方法的条件：
```python
# 连续失败 2 次则触发重启
if self.failed_counts[uid] >= 2:
```

## 注意事项

1. **全局创建锁**：重启 bridge 时会获取全局创建锁，期间其他账号无法创建新容器，这是正常现象。

2. **重启时间**：bridge 重启最多等待 10 分钟，如果超时会放弃本次重启，等待下个检测周期再试。

3. **资源消耗**：每次健康检查会消耗少量 Token（约 100 tokens），5 分钟一次，24 小时约 28,800 tokens。

4. **并发限制**：同一账号同时只会有一个健康检查任务在运行，避免重复检测。

5. **账号禁用**：如果账号被禁用或删除，对应的健康检查任务会自动停止。

## 与其他机制的配合

健康检查是**第三道防线**：

1. **第一道**：节点冷却机制（401 错误自动跳过 30 秒）
2. **第二道**：心跳超时检测（35 秒未心跳踢除僵尸节点）
3. **第三道**：健康检查（5 分钟定期主动测试 API 响应）

三道防线互为补充，确保服务的高可用性。
